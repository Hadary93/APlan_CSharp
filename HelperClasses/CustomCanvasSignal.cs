using System.Windows.Controls;

namespace APLan.HelperClasses
{
    /// <summary>
    /// Object to represent the Signals dragged from signals window or loaded in loading process.
    /// this is adjustable according to location.
    /// </summary>
    public class CustomCanvasSignal : Image
    {
        public CustomCanvasSignal()
        {
            // Empty constructor required to compile.
        }
    }
}
